# Leave this unset when deploying both services to Vercel: the frontend uses
# the shared-domain /api/v1 route. Set it only when using a separate API host.
VITE_API_URL=https://your-backend.example.com/api/v1
